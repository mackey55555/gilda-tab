export default function SalesPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold">売上集計</h1>
      <p className="mt-4 text-sm text-ink-muted">
        日別売上・商品別ランキング・時間帯別売上・CSV エクスポートは次のステップで実装します。
      </p>
    </div>
  );
}
